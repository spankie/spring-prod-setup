server:
  port: ${PORT:8080}
  servlet:
    context-path: "/api/v1"

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    # data:
    # - classpath:/data-test.sql